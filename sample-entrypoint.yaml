apiVersion: v1
kind: Pod
metadata:
  name: sample-entrypoint
spec:
  containers:
    - name: nginx-container-112
      image: nginx:1.12
      command: ["/bin/sleep"] # ENTRYPOINT 命令と同じ
      args: ["3600"] # CMD 命令と同じ